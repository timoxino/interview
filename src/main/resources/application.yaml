server:
  error:
    include-message: always
logging:
  level:
    org:
      springframework:
        data:
          neo4j: DEBUG
management:
  endpoints:
    web:
      base-path: /
      path-mapping:
        health: /
      exposure:
        include: httptrace
  endpoint:
    health:
      show-details: always